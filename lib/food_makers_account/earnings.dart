import 'package:flutter/material.dart';

class MakerEarnings extends StatelessWidget {
  const MakerEarnings({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Earnings"),);
  }
}